# Application frontend

To run in development mode, first install the dependencies:

```
npm install
```

Then run Gulp:

```
./node_modules/.bin/gulp
```

(You can also run `gulp` directly if you have installed it globally)
